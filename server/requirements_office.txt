# Dipendenze per conversione documenti Office → PDF
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.2.5
Pillow==10.4.0

# Conversione Office → PDF
unoconv==0.9.0
# ALTERNATIVA: libreoffice-python (se unoconv non funziona)
# comtypes==1.4.6  # Solo per Windows
